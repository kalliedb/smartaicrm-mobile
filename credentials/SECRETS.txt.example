# Copy this file to SECRETS.txt (gitignored) and fill in the values you
# chose when generating the keystore. Also mirror them into GitHub Actions
# secrets — see credentials/README.md.

ANDROID_KEY_ALIAS=aiverafield
ANDROID_KEYSTORE_PASSWORD=KbwrAndroidKeystore4820!
ANDROID_KEY_PASSWORD=<same-KbwrAndroidKeystore4820!

# ANDROID_KEYSTORE_BASE64 = contents of aivera-field-release.keystore.base64.txt
# (paste that whole file's contents into the GH secret, not this SECRETS.txt)
